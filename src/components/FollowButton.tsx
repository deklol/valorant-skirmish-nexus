interface FollowButtonProps {
  userId: string;
  className?: string;
}

export const FollowButton = ({ userId, className }: FollowButtonProps) => {
  // Placeholder component - will be fully implemented when database types are updated
  return null;
};